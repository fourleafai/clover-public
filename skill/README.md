# skill/

Claude Skill for interview prep — the low-friction install path for Claude Code / Claude Desktop users. **Coming soon.**

When shipped this folder will contain a `SKILL.md` and `references/` directory with commands for the full job-search lifecycle. Install by dropping the folder into `~/.claude/skills/four-leaf/`.

**Using a different AI assistant?** Install the [MCP server](../mcp) instead — same intelligence, works in ChatGPT Desktop, Cursor, Cline, Continue, Windsurf, and anywhere else MCP is supported.

See the [top-level README](../README.md) for the bigger picture.
